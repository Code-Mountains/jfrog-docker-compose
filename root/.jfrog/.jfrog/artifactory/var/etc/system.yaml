shared:
  node:
    ip: 127.0.0.1
    id: vault-rt
    name: vault-rt
  database:
    type: postgresql
    driver: org.postgresql.Driver
    url: jdbc:postgresql://127.0.0.1:5432/artifactory
    username: artifactory
    password: password
router:
  entrypoints:
    externalPort: 8082
